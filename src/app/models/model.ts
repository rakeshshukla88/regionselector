export interface Countries {
    numericCode: number;
    name: string;
    capital: string;
    population: any;
    currencies: {}[];
    flag: string;
}

export interface SelectionList { id: any; title: any }
